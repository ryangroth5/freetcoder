PWNED*
__pycache__/
*.py[co]
.pytest_cache/
.mypy_cache/
.ruff_cache/
node_modules/
dist/
.env
*.db
.venv/

# Playwright output (written through the dev bind mount)
frontend/test-results/
playwright-report/
.DS_Store
*.bak

# Bench artifacts: run output, not source.
bench-*.json
bench-*.log

# Agent/editor local state. `.claude/settings.local.json` was previously
# ignored only by a global ignore file, so it was invisible here and would have
# been committed by anyone cloning onto a machine without that rule.
.claude/
.idea/
.vscode/

*.sqlite
coverage*
htmlcov/
